#pragma once

// Win32 resource IDs embedded in efz_netplay_mod.dll (see src/netplay_mod.rc).

// Fallback menu font: Noto Sans CJK JP (TrueType conversion, OFL 1.1).
// Embedded so full Latin+Cyrillic+Japanese text rendering works even when
// the assets folder was not copied next to the DLL.
#define IDR_FONT_FALLBACK 201
